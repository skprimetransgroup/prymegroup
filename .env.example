# Pryme Group Environment Variables

# Database Configuration (Required)
# Get this from your Neon database or Replit secrets
DATABASE_URL=postgresql://user:password@host.region.neon.tech/database?sslmode=require

# PostgreSQL Connection Details (Optional - extracted from DATABASE_URL)
PGHOST=your-host.region.neon.tech
PGUSER=your-username
PGPASSWORD=your-password
PGDATABASE=your-database
PGPORT=5432

# Session Configuration (Required for Production)
# Generate with: openssl rand -base64 32
SESSION_SECRET=your-strong-random-secret-key-here

# Application Environment
NODE_ENV=development

# Optional: Force database usage in development
# Set to 'true' to use database instead of in-memory storage
USE_DB=false

# Server Port (for local development)
PORT=5000

# Replit Object Storage (if using)
# These are automatically set in Replit environment
# PUBLIC_OBJECT_SEARCH_PATHS=/public
# PRIVATE_OBJECT_DIR=/.private
